version: 2
updates:
- package-ecosystem: pip
  directory: "/"
  schedule:
    interval: daily
  open-pull-requests-limit: 10
  ignore:
  # torch's version is delft's to choose: it pins one exactly, so any bump
  # here is unsatisfiable until delft itself moves
  - dependency-name: torch
